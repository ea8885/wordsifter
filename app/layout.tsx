import type {Metadata} from "next";import "./globals.css";
export const metadata:Metadata={title:"WordSifter — Find the gold",description:"A songwriting workspace for comparing lyrics, preserving alternate lines, and finishing AI-assisted songs.",openGraph:{title:"WordSifter — Find the gold",description:"A songwriting workspace for comparing lyrics, preserving alternate lines, and finishing AI-assisted songs.",images:["/og.png"]},twitter:{card:"summary_large_image",title:"WordSifter — Find the gold",description:"A songwriting workspace for comparing lyrics, preserving alternate lines, and finishing AI-assisted songs.",images:["/og.png"]}};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
