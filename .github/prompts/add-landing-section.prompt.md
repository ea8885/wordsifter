---
description: Add a polished WordSifter landing-page section
agent: WordSifter Builder
---

Add the landing-page section described by the user.

First identify its purpose, ideal position in the current page flow, and one primary user action. Match the established WordSifter palette, typography, spacing, borders, and responsive grid behavior. Reuse existing patterns without duplicating copy. Implement desktop, tablet, and phone layouts; keep the section accessible; then validate the affected code.
